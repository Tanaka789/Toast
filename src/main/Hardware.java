package main;

public class Hardware {

    //contains cpu,gpy,memory array directly in it, audio, input, storage, RAM
    //each of has a memory position (in the memory held by the hardware) passed into itself and thats what it writes to
    public Hardware(){

    }
}
